@model web.Models.TableYouTubeAttribute

@{
    ViewBag.Title = "Delete";
    Layout = "~/Views/Shared/_LayoutRepresentation.cshtml";
}

<h2>Delete</h2>

<h3>Are you sure you want to delete this?</h3>
<fieldset>
    <legend>TableYouTubeAttribute</legend>

    <div class="display-label">TableDevice</div>
    <div class="display-field">
        @Html.DisplayFor(model => model.TableDevice.Name)
    </div>

    <div class="display-label">Description</div>
    <div class="display-field">
        @Html.DisplayFor(model => model.Description)
    </div>

    <div class="display-label">Title</div>
    <div class="display-field">
        @Html.DisplayFor(model => model.Title)
    </div>
</fieldset>
@using (Html.BeginForm()) {
    <p>
        <input type="submit" value="Delete" /> |
        @Html.ActionLink("Back to List", "Index")
    </p>
}
